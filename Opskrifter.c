#include <stdio.h>
#include "opskrift.h"

void initOpskrifter(Opskrift opskrifter[], int antal) {
    if (antal < 9) return;  // paramerter fra at den kan vælge mellem  9 retter:
    // Skriv kode fra her:

    // skriv kode, skal have navn på opskrift, tid ,indgredienser , fremgangsmåde, pris
}










